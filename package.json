{
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rimraf src/.observablehq/cache",
    "build": "rimraf dist && observable build",
    "dev": "observable preview",
    "predeploy": "npm run build",
    "deploy": "touch dist/.nojekyll && gh-pages -d dist -t true --cname cahaselby.github.io"
  },
  "dependencies": {
    "@observablehq/framework": "^1.10.1",
    "d3": "^7.9.0",
    "d3-dsv": "^3.0.1",
    "d3-sankey": "^0.12.3",
    "d3-time-format": "^4.1.0",
    "gh-pages": "^6.1.1"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  },
  "engines": {
    "node": ">=18"
  }
}
